version: '3.8'
services:
  postgres:
    image: postgres
    container_name: postgres-fazmerir
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: fazmerir
    restart: always
